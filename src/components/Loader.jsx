import React from 'react'
import './Styles/loader.css'

const Loader = ({item}) => {
    return (
        <section className='loader-contenedor'>
        <div class="loader">
            <span>AfterLife</span>
            <span>AfterLife</span>
        </div>
        </section>
        )
}

export default Loader